# Dependencies for pip install when numpy/astropy come from conda (environment.yml).
# Omit numpy and astropy so conda-forge versions are not overwritten (ABI compatibility).
# For pip-only install use: pip install -r requirements.txt then pip install -e .
astropy-healpix==0.7
astroquery==0.4.6
dask==2023.2.0
h5py==3.8.0
joblib==1.2.0
matplotlib==3.6.3
prox_tv==3.2.1
proxmin==0.6.12
pynufft==2023.1.1
PyWavelets==1.4.1
scipy==1.10.0
sparse>=0.14.0
